$('#like-form-<%=@post.id%>').html('<%= j render 'unlike_button', post_id: @post.id %>')

$("#likes-<%=@post.id%>").html('<%= @post.likes.count %> likes');

$('#like-button-<%=@post.id%>').html('<%= j render 'shared/unlike_button', post_id: @post.id %>')
