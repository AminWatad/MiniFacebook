<%= form_for(:like,
             url: likes_destroy_path,
             html: {method: :delete },
             remote: true) do |f| %>
  <div><%= hidden_field_tag :post_id, post_id %></div>           
  <%= f.submit "Unlike", class: "btn btn-danger btn-sm" %>
<% end %>

